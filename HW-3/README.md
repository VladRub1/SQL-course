## 👨🏻‍💻 ДЗ №3 Глава 4. Типы данных СУБД PostgreSQL

Содержание:
1. [Задание 2.](#задание_2)
2. [Задание 4.](#задание_4)
3. [Задание 8.](#задание_8)
4. [Задание 12.](#задание_12)
5. [Задание 15.](#задание_15)
6. [Задание 21.](#задание_21)
7. [Задание 30.](#задание_30)
8. [Задание 33.](#задание_33)
9. [Задание 35.](#задание_35)

### **Задание 2.** <a name="задание_2"></a>

<details>
<summary>🔽 Развернуть задание 🔽</summary>
Задание:

Предположим, что возникла необходимость хранить в одном столбце таблицы
данные, представленные с различной точностью. Это могут быть, например,
результаты физических измерений разнородных показателей или различные
медицинские показатели здоровья пациентов (результаты анализов). В таком
случае можно использовать тип `numeric` без указания масштаба и точности.
Команда для создания таблицы может быть, например, такой:

```SQL
CREATE TABLE test_numeric
( measurement numeric,
  description text
);
```

Вставьте в таблицу несколько строк:

```SQL
INSERT INTO test_numeric
  VALUES ( 1234567890.0987654321,
          'Точность 20 знаков, масштаб 10 знаков' );

INSERT INTO test_numeric
  VALUES ( 1.5,
          'Точность 2 знака, масштаб 1 знак' );

INSERT INTO test_numeric
  VALUES ( 0.12345678901234567890,
          'Точность 21 знак, масштаб 20 знаков' );

INSERT INTO test_numeric
  VALUES ( 1234567890,
          'Точность 10 знаков, масштаб 0 знаков (целое число)' );
```

Теперь сделайте выборку из таблицы и посмотрите, что все эти разнообразные
значения сохранены именно в том виде, как вы их вводили.
</details>

Ответ:

Как было предложено, создадим таблицу, в которой не будем указывать масштаб и точность
у типа данных `numeric`:
```SQL
CREATE TABLE test_numeric
( measurement numeric,
  description text
);
```

Посмотрим на описание с помощью команды `\d test_numeric`:
```
                        Таблица "bookings.test_numeric"
   Столбец   |   Тип   | Правило сортировки | Допустимость NULL | По умолчанию 
-------------+---------+--------------------+-------------------+--------------
 measurement | numeric |                    |                   | 
 description | text    |                    |                   | 
```

Действительно, у типа `numeric` нет информации про масштаб и точность. 
Также помним, что тип `text` фактически не имеет ограничений.

Теперь попробуем ввести данные в таблицу и посмотреть на нее:
```SQL
INSERT INTO test_numeric
  VALUES ( 1234567890.0987654321,
          'Точность 20 знаков, масштаб 10 знаков' );
INSERT INTO test_numeric
  VALUES ( 1.5,
          'Точность 2 знака, масштаб 1 знак' );
INSERT INTO test_numeric
  VALUES ( 0.12345678901234567890,
          'Точность 21 знак, масштаб 20 знаков' );
INSERT INTO test_numeric
  VALUES ( 1234567890,
          'Точность 10 знаков, масштаб 0 знаков (целое число)' );
INSERT INTO test_numeric
  VALUES ( 12.34,
          'Точность 4 знака, масштаб 2 знака' );
INSERT INTO test_numeric
  VALUES ( 1234567890123456789012345678901234567890.1234567890,
          'Точность 50 знаков, масштаб 10 знаков' );
          
SELECT * FROM test_numeric;
```

Вывод:
```
                     measurement                     |                    description                     
-----------------------------------------------------+----------------------------------------------------
                               1234567890.0987654321 | Точность 20 знаков, масштаб 10 знаков
                                                 1.5 | Точность 2 знака, масштаб 1 знак
                              0.12345678901234567890 | Точность 21 знак, масштаб 20 знаков
                                          1234567890 | Точность 10 знаков, масштаб 0 знаков (целое число)
                                               12.34 | Точность 4 знака, масштаб 2 знака
 1234567890123456789012345678901234567890.1234567890 | Точность 50 знаков, масштаб 10 знаков

```
Действительно, в одном поле `measurement` находятся строки с различными точностью и масштабом:
от целых чисел до 20 знаков после целой части.

---
### **Задание 4.** <a name="задание_4"></a>

<details>
<summary>🔽 Развернуть задание 🔽</summary>
Задание:

При работе с числами типов `real` и `double precision` нужно помнить, что
сравнение двух чисел с плавающей точкой на предмет равенства их значений
может привести к неожиданным результатам.

Например, сравним два очень маленьких числа (они представлены в экспоненциальной 
форме записи):

```SQL
SELECT '5e-324'::double precision > '4e-324'::double precision;

?column?
----------
f
(1 строка)
```

Чтобы понять, почему так получается, выполните еще два запроса.
```SQL
SELECT '5e-324'::double precision;

float8
-----------------------
4.94065645841247e-324
(1 строка)

SELECT '4e-324'::double precision;

float8
-----------------------
4.94065645841247e-324
(1 строка)
```

Самостоятельно проведите аналогичные эксперименты с очень большими 
числами, находящимися на границе допустимого диапазона для чисел типов `real`
и `double precision`.
</details>

Ответ:

Получить некорректную работу с очень большими числами оказалось сложнее, 
чем с очень маленькими, т.к. они быстрее вызывают переполнение.

Пример с типом `real`:
```SQL
SELECT '3.0000498e+38'::real < '3.0000499e+38'::real ;

 ?column? 
----------
 f
(1 строка)
```

Посмотрим внимательнее: 
```SQL
SELECT '3.0000498e+38'::real ;

   float4    
-------------
 3.00005e+38
(1 строка)

SELECT '3.0000499e+38'::real ;

   float4    
-------------
 3.00005e+38
```
Числа, округляясь, хранятся одинаково!

Для `double precision` получилось вызвать нелогичный результат только 
с применением мат. операций:
```SQL
SELECT '1.001e+308'::double precision + 
       '1e+10'::double precision < '1.002e+308'::double precision ;

 ?column? 
----------
 t
(1 строка)
```

Посмотрим внимательнее: 
```SQL
SELECT '1.001e+308'::double precision + '1e+10'::double precision ;

  ?column?  
------------
 1.001e+308
(1 строка)

SELECT '1.002e+308'::double precision ;

   float8   
------------
 1.002e+308
(1 строка)
```
Видно, что сложения не происходит, если потенциально 
произойдет переполнение.

Вычитание при больших значениях типа `double precision`:
```SQL
SELECT '1e+308'::double precision - 
       '1e+10'::double precision > '1.001e+307'::double precision ;
       
 ?column? 
----------
 t
(1 строка)
```

Посмотрим по отдельности:
```SQL
SELECT '1e+308'::double precision - '1e+10'::double precision ;

 ?column? 
----------
   1e+308
(1 строка)

SELECT '1.001e+307'::double precision ;

   float8   
------------
 1.001e+307
```
Видим, что вычитания также не происходит.

---
### **Задание 8.** <a name="задание_8"></a>

<details>
<summary>🔽 Развернуть задание 🔽</summary>
Задание:

Немного усложним определение таблицы из предыдущего задания. Пусть 
теперь столбец id будет первичным ключом этой таблицы.

```SQL
CREATE TABLE test_serial
( id serial PRIMARY KEY,
  name text
);
```

Теперь выполните следующие команды для добавления строк в таблицу и 
удаления одной строки из нее. Для пошагового управления этим процессом 
выполняйте выборку данных из таблицы с помощью команды SELECT после каждой
команды вставки или удаления.

```SQL
INSERT INTO test_serial ( name ) VALUES ( 'Вишневая' );
```

Явно зададим значение столбца id:

```SQL
INSERT INTO test_serial ( id, name ) VALUES ( 2, 'Прохладная' );
```

При выполнении этой команды СУБД выдаст сообщение об ошибке. Почему?

```SQL
INSERT INTO test_serial ( name ) VALUES ( 'Грушевая' );
```

Повторим эту же команду. Теперь все в порядке. Почему?

```SQL
INSERT INTO test_serial ( name ) VALUES ( 'Грушевая' );
```

Добавим еще одну строку.

```SQL
INSERT INTO test_serial ( name ) VALUES ( 'Зеленая' );
```

А теперь удалим ее же.

```SQL
DELETE FROM test_serial WHERE id = 4;
```

Добавим последнюю строку.

```SQL
INSERT INTO test_serial ( name ) VALUES ( 'Луговая' );
```

Теперь сделаем выборку.

```SQL
SELECT * FROM test_serial;
```

Вы увидите, что в нумерации образовалась «дыра». Это из-за того, что при 
формировании нового значения из последовательности поиск максимального 
значения, уже имеющегося в столбце, не выполняется.

```
id  name
----+------------
1  Вишневая
2  Прохладная
3  Грушевая
5  Луговая
(4 строки)
```
</details>

Ответ:

После создания тестовой таблицы предложенным запросом: 
```SQL
CREATE TABLE test_serial
( id serial PRIMARY KEY,
  name text
);
```

Посмотрим на ее описание:
```SQL
\d test_serial

                                    Таблица "bookings.test_serial"
 Столбец |   Тип   | Правило сортировки | Допустимость NULL |              По умолчанию               
---------+---------+--------------------+-------------------+-----------------------------------------
 id      | integer |                    | not null          | nextval('test_serial_id_seq'::regclass)
 name    | text    |                    |                   | 
Индексы:
    "test_serial_pkey" PRIMARY KEY, btree (id)
```

Видно, что для поля `id` по умолчанию создалась последовательность, 
которая будет вызываться каждый раз при ее вызове в случае,
если мы напрямую не укажем значение этого поля.

Поэтому, когда мы в начале добавим первую строку,
указав только значение поля `name`, в качестве значения поля `id`
автоматически подставится 1.
Когда мы во второй раз укажем, что значение поля `id` 
равно 2 напрямую, все сработает корректно для нас:

```SQL
INSERT INTO test_serial ( name ) VALUES ( 'Вишневая' );
INSERT INTO test_serial ( id, name ) VALUES ( 2, 'Прохладная' );
SELECT * FROM test_serial;
```

```
 id |    name    
----+------------
  1 | Вишневая
  2 | Прохладная
(2 строки)
```

Но, когда мы в следующий раз не подставим ничего вручную в поле `id`:
```SQL
INSERT INTO test_serial ( name ) VALUES ( 'Грушевая' );
```

снова активируется генератор последовательности, остановившийся на 
числе 2 и предложит подставить его, ничего не добавив и 
вызвав ошибку:
```
ОШИБКА:  повторяющееся значение ключа нарушает ограничение 
уникальности "test_serial_pkey"
```

т.к. такое значение в поле `id` уже есть, а мы при этом поставили ограничение
`PRIMARY KEY` на это поле, чтобы все его значения были уникальными.

При следующем вызове он "прокрутится" еще раз и не вызовет ошибку,
поскольку значения 3 в поле `id` уже не будет:

```SQL
INSERT INTO test_serial ( name ) VALUES ( 'Грушевая' );
INSERT INTO test_serial ( name ) VALUES ( 'Зеленая' );
SELECT * FROM test_serial ;
```

Грушевая улица добавится в таблицу, как и Зеленая:
```
 id |    name    
----+------------
  1 | Вишневая
  2 | Прохладная
  3 | Грушевая
  4 | Зеленая
(4 строки)
```

Теперь очевидно, что, если удалить последнюю строку с `id = 4`
и еще раз активировать наш генератор последовательности типа 
`serial`, он в качестве следующего значения для поля `id`
вставит 5, поскольку он не обращает внимания на то, что
в текущей таблице "пропущено" значение 4, если смотреть по порядку, 
максимальным значением на текущий момент является 3.
```SQL
DELETE FROM test_serial WHERE id = 4;
SELECT * FROM test_serial;
```

```
 id |    name    
----+------------
  1 | Вишневая
  2 | Прохладная
  3 | Грушевая
(3 строки)
```

```SQL
INSERT INTO test_serial ( name ) VALUES ( 'Луговая' );
SELECT * FROM test_serial;
```

```
 id |    name    
----+------------
  1 | Вишневая
  2 | Прохладная
  3 | Грушевая
  5 | Луговая
(4 строки)
```
Таким образом, генератор, созданный с помощью типа `serial` активируется каждый раз,
когда мы не указываем никакого значения соответствующего поля, а также он не "смотрит"
на другие значения и каждый раз при вызове пытается подставить следующий элемент
последовательности.

---
### **Задание 12.** <a name="задание_12"></a>

<details>
<summary>🔽 Развернуть задание 🔽</summary>
Задание:

Формат ввода и вывода даты можно изменить с помощью конфигурационного
параметра `datestyle`. Значение этого параметра состоит из двух 
компонентов: первый управляет форматом вывода даты, а второй регулирует 
порядок следования составных частей даты (год, месяц, день) при вводе и
выводе. Текущее значение этого параметра можно узнать с помощью команды 
SHOW:

```SQL
SHOW datestyle;
```
По умолчанию он имеет такое значение:

```
DateStyle
-----------
ISO, DMY
(1 строка)
```
Продемонстрируем влияние этого параметра на работу с типами данных `date`
и `timestamp`. Для экспериментов возьмем дату, в которой число (день) 
превышает 12, чтобы нельзя было день перепутать с номером месяца. 
Пусть это будет, например, 18 мая 2016 г.

```SQL
SELECT '18-05-2016'::date;
```

Хотя порядок следования составных частей даты задан в виде DMY, 
т. е. «день,
месяц, год», но при выводе он изменяется на «год, месяц, день».
```
date
------------
2016-05-18
(1 строка)
```

Попробуем ввести дату в порядке «месяц, день, год»:

```SQL
SELECT '05-18-2016'::date;
```

В ответ получим сообщение об ошибке. Если бы мы выбрали дату, в которой
число (день) было бы не больше 12, например, 9, то сообщение об ошибке не
было бы сформировано, т. е. мы с такой датой не смогли бы проиллюстрировать
влияние значения DMY параметра `datestyle`. Но главное, что в таком 
случае мы бы просто не заметили допущенной ошибки.

А вот использовать порядок «год, месяц, день» при вводе можно несмотря на
то, что параметр `datestyle` предписывает «день, месяц, год». Порядок «год,
месяц, день» является универсальным, его можно использовать всегда, 
независимо от настроек параметра `datestyle`.

```SQL
SELECT '2016-05-18'::date;
```
```
date
------------
2016-05-18
(1 строка)
```

Продолжим экспериментирование с параметром `datestyle`. Давайте изменим
его значение. Сделать это можно многими способами, но мы упомянем лишь
некоторые:
- изменив его значение в конфигурационном файле `postgresql.conf`, который
в нашей инсталляции PostgreSQL, описанной в главе 2, находится в каталоге
/usr/local/pgsql/data;
- назначив переменную системного окружения `PGDATESTYLE`;
- воспользовавшись командой `SET`.

Сейчас выберем третий способ, а первые два рассмотрим при выполнении 
других заданий. Поскольку параметр `datestyle` состоит фактически из двух 
частей, которые можно задавать не только обе сразу, но и по отдельности, 
изменим только порядок следования составных частей даты, не изменяя формат
вывода с `ISO` на какой-либо другой.

```SQL
SET datestyle TO 'MDY';
```

Повторим одну из команд, выполненных ранее. Теперь она должна вызвать
ошибку. Почему?

```SQL
SELECT '18-05-2016'::date;
```

А такая команда, наоборот, теперь будет успешно выполнена:

```SQL
SELECT '05-18-2016'::date;
```

Теперь приведите настройку параметра `datestyle` в исходное состояние:

```SQL
SET datestyle TO DEFAULT;
```

Самостоятельно выполните команды `SELECT`, приведенные выше, но замените 
в них тип date на тип timestamp. Вы увидите, что дата в рамках типа
timestamp обрабатывается аналогично типу date.
Сейчас изменим сразу обе части параметра `datestyle`:

```SQL
SET datestyle TO 'Postgres, DMY';
```

Проверьте полученный результат с помощью команды `SHOW`.

Самостоятельно выполните команды `SELECT`, приведенные выше, как для 
значения типа `date`, так и для значения типа `timestamp`. 
Обратите внимание, что
если выбран формат Postgres, то порядок следования составных частей даты
(день, месяц, год), заданный в параметре `datestyle`, используется не 
только при вводе значений, но и при выводе. Напомним, что вводом мы считаем
команду `SELECT`, а выводом — результат ее выполнения, выведенный на экран.

В документации (см. раздел 8.5.2 «Вывод даты/времени») сказано, что формат
вывода даты может принимать значения `ISO`, `Postgres`, `SQL` и `German`. 
Первые два варианта мы уже рассмотрели. Самостоятельно поэкспериментируйте
с двумя оставшимися по той же схеме, по которой вы уже действовали ранее
при выполнении этого задания. Можно воспользоваться и стандартными 
функциями `current_date` и `current_timestamp`.
</details>

Ответ:

Посмотрим, как сейчас выглядит конфигурационный параметр `datestyle`:
```SQL
SHOW datestyle;

 DateStyle 
-----------
 ISO, DMY
(1 строка)
```
Как и должен выглядеть по умолчанию.

Возьмем, как было предложено, 18 мая 2016 г. для примера.
```SQL
SELECT '18-05-2016'::date;

    date    
------------
 2016-05-18
(1 строка)
```
Действительно, формат ввода работает как день-месяц-год (DMY), 
а формат вывода - как год-месяц-день (ISO).

Теперь попробуем изменить формат ввода:
```SQL
SET datestyle TO 'MDY';
```
Далее введем:
```SQL
SELECT '18-05-2016'::date;

ОШИБКА:  значение поля типа date/time вне диапазона: "18-05-2016"
СТРОКА 1: SELECT '18-05-2016'::date;
                 ^
ПОДСКАЗКА:  Возможно, вам нужно изменить настройку "datestyle".
```
Теперь этот запрос вызывает ошибку, т.к. мы поменяли формат ввода, а 
месяца под номером 18 не существует. СУБД тоже говорит об этом.

Эта команда выполнится успешно:
```SQL
SELECT '05-18-2016'::date;

    date    
------------
 2016-05-18
(1 строка)
```
Вернем настройку параметра `datestyle` в исходное состояние:
```SQL
SET datestyle TO DEFAULT;
```

Далее попробуем сделать похожий эксперимент с типом данных `timestamp`,
добавив часы, минуты и секунды.
```SQL
SELECT '18-05-2016 22:19:03'::timestamp;

      timestamp      
---------------------
 2016-05-18 22:19:03
(1 строка)
```
Действительно, работает так же, как и с `date`.

Попробуем ввести дату в порядке «месяц, день, год»:
```SQL
SELECT '05-18-2016 22:19:03'::timestamp;

ОШИБКА:  значение поля типа date/time вне диапазона: "05-18-2016 22:19:03"
СТРОКА 1: SELECT '05-18-2016 22:19:03'::timestamp;
                 ^
ПОДСКАЗКА:  Возможно, вам нужно изменить настройку "datestyle".
```
В ответ получили ту же ошибку.

Попробуем ввести универсальный формат «год, месяц, день»:

```SQL
SELECT '2016-05-18 22:19:03'::timestamp;

      timestamp      
---------------------
 2016-05-18 22:19:03
(1 строка)
```
Все получилось.

Далее поменяем настройки параметра `datestyle`:
```SQL
SET datestyle TO 'MDY';
```
Повторим одну из команд, выполненных ранее.
```SQL
SELECT '18-05-2016 22:19:03'::timestamp;

ОШИБКА:  значение поля типа date/time вне диапазона: "18-05-2016 22:19:03"
СТРОКА 1: SELECT '18-05-2016 22:19:03'::timestamp;
                 ^
ПОДСКАЗКА:  Возможно, вам нужно изменить настройку "datestyle".
```

Теперь уже для нее получаем ошибку.
Другая команда, как и с `date`, будет успешно выполнена:
```SQL
SELECT '05-18-2016 22:19:03'::timestamp;

      timestamp      
---------------------
 2016-05-18 22:19:03
(1 строка)
```
Вернем настройку параметра `datestyle` в исходное состояние:
```SQL
SET datestyle TO DEFAULT;
```

Сейчас изменим сразу обе части параметра `datestyle`:

```SQL
SET datestyle TO 'Postgres, DMY';
```
Проверим полученный результат с помощью команды `SHOW`:
```SQL
SHOW datestyle;

   DateStyle   
---------------
 Postgres, DMY
(1 строка)
```
Видно, что изменились обе части. Проделаем те же операции.

```SQL
SELECT '18-05-2016'::date;

    date    
------------
 18-05-2016
(1 строка)

SELECT '18-05-2016 22:19:03'::timestamp;

        timestamp         
--------------------------
 Wed 18 May 22:19:03 2016
(1 строка)
```
Видим изменения:
1. Формат `date` выводится в том же формате, что и вводится («день,
месяц, год»).
2. Формат `timestamp` считывается так же, но формат вывода теперь выглядит
по-другому: «День недели, число, месяц (буквами), время, год».

Попытки ввести `date` и `timestamp` в порядке «месяц, день, год» 
закончились той же ошибкой:

```SQL
SELECT '05-18-2016'::date;
ОШИБКА:  значение поля типа date/time вне диапазона: "05-18-2016"
СТРОКА 1: SELECT '05-18-2016'::date;
                 ^
ПОДСКАЗКА:  Возможно, вам нужно изменить настройку "datestyle".

SELECT '05-18-2016 22:19:03'::timestamp;
ОШИБКА:  значение поля типа date/time вне диапазона: "05-18-2016 22:19:03"
СТРОКА 1: SELECT '05-18-2016 22:19:03'::timestamp;
                 ^
ПОДСКАЗКА:  Возможно, вам нужно изменить настройку "datestyle".
```

Попробуем использовать универсальный порядок 
«год, месяц, день» при вводе:
```SQL
SELECT '2016-05-18'::date;

    date    
------------
 18-05-2016
(1 строка)

SELECT '2016-05-18 22:19:03'::timestamp;

        timestamp         
--------------------------
 Wed 18 May 22:19:03 2016
(1 строка)
```
Как утверждалось выше, его действительно можно использовать всегда, 
независимо от настроек параметра `datestyle`.

Поменяем вторую часть параметра `datestyle`:
```SQL
SET datestyle TO 'MDY';
SHOW datestyle;

   DateStyle   
---------------
 Postgres, MDY
(1 строка)
```
Команды
```SQL
SELECT '18-05-2016'::date;
SELECT '18-05-2016 22:19:03'::timestamp;
```
привычно вызывают ошибку.

Команды ниже, наоборот, теперь будут успешно выполнены:
```SQL
SELECT '05-18-2016'::date;

    date    
------------
 05-18-2016
(1 строка)

SELECT '05-18-2016 22:19:03'::timestamp;

        timestamp         
--------------------------
 Wed May 18 22:19:03 2016
(1 строка)
```
Вывод остался новым, согласно стилю `Postgres`.

Вернем настройку параметра `datestyle` в исходное состояние:

```SQL
SET datestyle TO DEFAULT;
```

Далее поэкспериментируем с форматами вывода даты `SQL` и `German`.
```SQL
SET datestyle TO 'SQL, DMY';
```
```SQL
SELECT current_date::date;

 current_date 
--------------
 30/09/2023
(1 строка)
```
Видим, что формат `date` выводится в формате «день, месяц, год»,
но числа разделяются через `/`.

Посмотрим на `timestamp`:
```SQL
SELECT current_timestamp::timestamp;
     current_timestamp      
----------------------------
 30/09/2023 12:29:39.145472
(1 строка)
```
дата в `timestamp` выводится так же, а время - как в других стилях.
Форматы ввода даты `DMY` и `MDY` работают аналогично.

Установим другой формат вывода:
```SQL
SET datestyle TO 'German, DMY';
```
Теперь `date` и `timestamp` выглядят так:
```SQL
SELECT current_date::date;

 current_date 
--------------
 30.09.2023
(1 строка)

SELECT current_timestamp::timestamp;

     current_timestamp      
----------------------------
 30.09.2023 12:34:52.969193
(1 строка)
```
Дата разделяется через точки, как это принято в российской 
типографской традиции.

---
### **Задание 15.** <a name="задание_15"></a>

<details>
<summary>🔽 Развернуть задание 🔽</summary>
Задание:

В документации в разделе 9.8 «Функции форматирования данных» 
представлены описания множества полезных функций, позволяющих преобразовать
в строку данные других типов, например, `timestamp`. Одна из таких функций — 
`to_char`.

Приведем несколько команд, иллюстрирующих использование этой функции.
Ее первым параметром является форматируемое значение, а вторым — шаблон,
описывающий формат, в котором это значение будет представлено при вводе
или выводе. Сначала попробуйте разобраться, не обращаясь к документации,
в том, что означает второй параметр этой функции в каждой из приведенных
команд, а затем проверьте свои предположения по документации.

```SQL
SELECT to_char( current_timestamp, 'mi:ss' );

to_char
---------
47:43
(1 строка)
```

```SQL
SELECT to_char( current_timestamp, 'dd' );
to_char
---------
12
(1 строка)
```

```SQL
SELECT to_char( current_timestamp, 'yyyy-mm-dd' );
to_char
------------
2017-03-12
(1 строка)
```

Поэкспериментируйте с этой функцией, извлекая из значения типа `timestamp`
различные поля и располагая их в нужном вам порядке
</details>

Ответ:

В ходе экспериментов было выяснено, что вывод функции `current_timestamp`:
```SQL
SELECT current_timestamp;

       current_timestamp       
-------------------------------
 2023-09-30 12:50:11.570121+03
(1 строка)
```
практически соответствует следующему паттерну:
```SQL
SELECT to_char( current_timestamp, 'yyyy-mm-dd hh:mi:ss.us OF' );
            to_char             
--------------------------------
 2023-09-30 12:50:08.376700 +03
(1 строка)
```
Например, можно вывести текущую дату, часы и минуты:
```SQL
SELECT to_char( current_timestamp, 'yyyy-mm-dd hh:mi' );

     to_char      
------------------
 2023-09-30 12:51
(1 строка)
```
Еще можно попробовать вывести название месяца буквами:
```SQL
SELECT to_char( current_timestamp, 'yyyy-Mon-dd hh:mi' );

      to_char      
-------------------
 2023-Sep-30 12:51
(1 строка)
```
Можно добавить название дня недели:
```SQL
SELECT to_char( current_timestamp, 'yyyy-Mon-dd Day hh:mi' );

           to_char           
-----------------------------
 2023-Sep-30 Saturday  12:54
(1 строка)
```
Или поменять разделитель и порядок вывода дня, месяца и года:
```SQL
SELECT to_char( current_timestamp, 'dd/Mon/yyyy Day hh:mi' );

           to_char           
-----------------------------
 30/Sep/2023 Saturday  12:56
```
---
### **Задание 21.** <a name="задание_21"></a>

<details>
<summary>🔽 Развернуть задание 🔽</summary>
Задание:

Можно с высокой степенью уверенности предположить, что при прибавлении
интервалов к датам и временным м отметкам PostgreSQL учитывает тот факт,
что различные месяцы имеют различное число дней. Но как это реализуется
на практике? Например, что получится при прибавлении интервала в 1 месяц
к последнему дню января и к последнему дню февраля? Сначала сделайте 
обоснованные предположения о результатах следующих двух команд, а затем 
проверьте предположения на практике и проанализируйте полученные результаты:

```SQL
SELECT ( '2016-01-31'::date + '1 mon'::interval ) AS new_date;
SELECT ( '2016-02-29'::date + '1 mon'::interval ) AS new_date;
```
</details>

Ответ:

Я думаю, что было бы логично, если бы при прибавлении месяца к некоторому 
числу месяца, выводилось бы соответствующее число из следующего месяца:
первое - первое, последнее - последнее, серединное - серединное,
поскольку мне кажется, что люди подразумевают именно это, когда
говорят: "через месяц", аналогично это должно работать в 
юридических договорах.

Проверим гипотезу:
```SQL
SELECT ( '2016-01-31'::date + '1 mon'::interval ) AS new_date;

      new_date       
---------------------
 2016-02-29 00:00:00
(1 строка)
```
```SQL
SELECT ( '2016-02-29'::date + '1 mon'::interval ) AS new_date;

      new_date       
---------------------
 2016-03-29 00:00:00
(1 строка)
```
В первом случае получилось логично: последний день января + 1 месяц = последний 
день февраля.
Но во втором случае, как мне кажется, не совсем логично: последний день 
февраля + 1 месяц = 2 дня до последнего дня марта. Лично мне хотелось бы увидеть там 
31 марта.

---
### **Задание 30.** <a name="задание_30"></a>

<details>
<summary>🔽 Развернуть задание 🔽</summary>
Задание:

Обратимся к таблице, создаваемой с помощью команды
```SQL
CREATE TABLE test_bool
( a boolean,
  b text
);
```
Как вы думаете, какие из приведенных ниже команд содержат ошибку?
```SQL
INSERT INTO test_bool VALUES ( TRUE, 'yes' );
INSERT INTO test_bool VALUES ( yes, 'yes' );
INSERT INTO test_bool VALUES ( 'yes', true );
INSERT INTO test_bool VALUES ( 'yes', TRUE );
INSERT INTO test_bool VALUES ( '1', 'true' );
INSERT INTO test_bool VALUES ( 1, 'true' );
INSERT INTO test_bool VALUES ( 't', 'true' );
INSERT INTO test_bool VALUES ( 't', truth );
INSERT INTO test_bool VALUES ( true, true );
INSERT INTO test_bool VALUES ( 1::boolean, 'true' );
INSERT INTO test_bool VALUES ( 111::boolean, 'true' );
```
Проверьте свои предположения практически, выполнив эти команды.
</details>

Ответ:

Создадим таблицу:
```SQL
CREATE TABLE test_bool
( a boolean,
  b text
);
```

Думаю, что, из предложенных команд, ошибку должны вызвать следующие:
- `INSERT INTO test_bool VALUES ( yes, 'yes' );`, поскольку у типа `boolean`
нет значения `yes`, нужно обрамить это слово в кавычки: `'yes'`
- `INSERT INTO test_bool VALUES ( 1, 'true' );`, т.к. значение 
`TRUE` соответствует `'1'`, а не `1` (int)
- `INSERT INTO test_bool VALUES ( 't', truth );`, т.к. значение `truth` не 
заключено в кавычки и будет восприниматься как столбец
- `INSERT INTO test_bool VALUES ( 111::boolean, 'true' );`, т.к. 
значению `TRUE` для приведения в тип boolean должно соответствовать значение 
`1`, а не `111`

**Итог**: я был прав насчет всех запросов, кроме последнего. Оказалось, что 
он успешно проходил, и `111::boolean` превращалось в `TRUE`

В целом, при ошибках было два следующих типа комментариев:
```
ОШИБКА:  столбец "yes" не существует
СТРОКА 1: INSERT INTO test_bool VALUES ( yes, 'yes' );
                                         ^
```
и
```
ОШИБКА:  столбец "a" имеет тип boolean, а выражение - integer
СТРОКА 1: INSERT INTO test_bool VALUES ( 1, 'true' );
                                         ^
ПОДСКАЗКА:  Перепишите выражение или преобразуйте его тип.
```
---
### **Задание 33.** <a name="задание_33"></a>

<details>
<summary>🔽 Развернуть задание 🔽</summary>
Задание:

В разделе документации 8.15 «Массивы» сказано, что массивы могут быть 
многомерными и в них могут содержаться значения любых типов. Давайте 
сначала рассмотрим одномерные массивы текстовых значений.

Предположим, что пилоты авиакомпании имеют возможность высказывать
свои пожелания насчет конкретных блюд, из которых должен состоять их обед
во время полета. Для учета пожеланий пилотов необходимо модифицировать
таблицу `pilots`, с которой мы работали в разделе 4.5.

```SQL
CREATE TABLE pilots
( pilot_name text,
  schedule integer[],
  meal text[]
);
```

Добавим строки в таблицу:

```SQL
INSERT INTO pilots
VALUES ( 'Ivan', '{ 1, 3, 5, 6, 7 }'::integer[],
         '{ "сосиска", "макароны", "кофе" }'::text[]
       ),
       ( 'Petr', '{ 1, 2, 5, 7 }'::integer [],
         '{ "котлета", "каша", "кофе" }'::text[]
       ),
       ( 'Pavel', '{ 2, 5 }'::integer[],
         '{ "сосиска", "каша", "кофе" }'::text[]
       ),
       ( 'Boris', '{ 3, 5, 6 }'::integer[],
         '{ "котлета", "каша", "чай" }'::text[]
       );
INSERT 0 4
```
Обратите внимание, что каждое из текстовых значений, включаемых в литерал
массива, заключается в двойные кавычки, а в качестве типа данных указывается
`text[]`.

Вот что получилось:

```SQL
SELECT * FROM pilots;

 pilot_name     schedule             meal
------------+-------------+-------------------------
 Ivan        {1,3,5,6,7}  {сосиска,макароны,кофе}
 Petr        {1,2,5,7}    {котлета,каша,кофе}
 Pavel       {2,5}        {сосиска,каша,кофе}
 Boris       {3,5,6}      {котлета,каша,чай}
(4 строки)
```

Давайте получим список пилотов, предпочитающих на обед сосиски:

```SQL
SELECT * FROM pilots WHERE meal[ 1 ] = 'сосиска';

 pilot_name     schedule             meal
------------+-------------+-------------------------
 Ivan        {1,3,5,6,7}   {сосиска,макароны,кофе}
 Pavel       {2,5}         {сосиска,каша,кофе}
(2 строки)
```

Предположим, что руководство авиакомпании решило, что пища пилотов должна
быть разнообразной. Оно позволило им выбрать свой рацион на каждый
из четырех дней недели, в которые пилоты совершают полеты. Для нас это
решение руководства выливается в необходимость модифицировать таблицу,
а именно: столбец `meal` теперь будет содержать двумерные массивы. 
Определение этого столбца станет таким: `meal text[][]`.

Задание. Создайте новую версию таблицы и соответственно измените команду 
`INSERT`, чтобы в ней содержались литералы двумерных массивов. Они будут
выглядеть примерно так:

```
'{ { "сосиска", "макароны", "кофе" },
   { "котлета", "каша", "кофе" },
   { "сосиска", "каша", "кофе" },
   { "котлета", "каша", "чай" } }'::text[][]
```

Сделайте ряд выборок и обновлений строк в этой таблице. Для обращения к 
элементам двумерного массива нужно использовать два индекса. Не забывайте,
что по умолчанию номера индексов начинаются с единицы.
</details>

Ответ:

Создадим таблицу и введем в нее значения:
```SQL
CREATE TABLE pilots
( pilot_name text,
  schedule integer[],
  meal text[]
);

INSERT INTO pilots
VALUES ( 'Ivan', '{ 1, 3, 5, 6, 7 }'::integer[],
         '{ "сосиска", "макароны", "кофе" }'::text[]
       ),
       ( 'Petr', '{ 1, 2, 5, 7 }'::integer [],
         '{ "котлета", "каша", "кофе" }'::text[]
       ),
       ( 'Pavel', '{ 2, 5 }'::integer[],
         '{ "сосиска", "каша", "кофе" }'::text[]
       ),
       ( 'Boris', '{ 3, 5, 6 }'::integer[],
         '{ "котлета", "каша", "чай" }'::text[]
       );

SELECT * FROM pilots;
```
Посмотрим, на одномерные массивы:
```
 pilot_name |  schedule   |          meal           
------------+-------------+-------------------------
 Ivan       | {1,3,5,6,7} | {сосиска,макароны,кофе}
 Petr       | {1,2,5,7}   | {котлета,каша,кофе}
 Pavel      | {2,5}       | {сосиска,каша,кофе}
 Boris      | {3,5,6}     | {котлета,каша,чай}
(4 строки)
```
Теперь модернизируем таблицу и добавим возможность указывать разные блюда
в каждый из дней работы пилотов:
```SQL
CREATE TABLE pilots_new
( pilot_name text,
schedule integer[],
meal text[][]
);
```
Введем по 4 разных набора еды для каждого из пилотов на каждый из его 4 
рабочих дней:
```SQL
INSERT INTO pilots_new
VALUES ( 'Ivan', '{ 1, 5, 6, 7 }'::integer[],
         '{ { "сосиска", "макароны", "кофе" },
            { "котлета", "макароны", "чай" },
            { "котлета", "каша", "чай" },
            { "сосиска", "макароны", "кофе" } }'::text[][]
       ),
       ( 'Petr', '{ 1, 2, 5, 7 }'::integer [],
         '{ { "котлета", "каша", "кофе" },
            { "котлета", "макароны", "чай" },
            { "сосиска", "макароны", "чай" },
            { "сосиска", "каша", "кофе" } }'::text[][]
       ), 
       ( 'Pavel', '{ 2, 3, 4, 5 }'::integer[],
         '{ { "сосиска", "макароны", "кофе" },
            { "сосиска", "каша", "кофе" },
            { "сосиска", "макароны", "кофе" },
            { "котлета", "каша", "чай" } }'::text[][]
       ),
       ( 'Boris', '{ 1, 3, 5, 7 }'::integer[],
         '{ { "сосиска", "макароны", "чай" },
            { "котлета", "макароны", "кофе" },
            { "сосиска", "макароны", "кофе" },
            { "сосиска", "макароны", "чай" } }'::text[][]
       );
```
Посмотрим, что получилось:
```SQL
SELECT * FROM pilots_new;

 pilot_name | schedule  |                                              meal                                               
------------+-----------+-------------------------------------------------------------------------------------------------
 Ivan       | {1,5,6,7} | {{сосиска,макароны,кофе},{котлета,макароны,чай},{котлета,каша,чай},{сосиска,макароны,кофе}}
 Petr       | {1,2,5,7} | {{котлета,каша,кофе},{котлета,макароны,чай},{сосиска,макароны,чай},{сосиска,каша,кофе}}
 Pavel      | {2,3,4,5} | {{сосиска,макароны,кофе},{сосиска,каша,кофе},{сосиска,макароны,кофе},{котлета,каша,чай}}
 Boris      | {1,3,5,7} | {{сосиска,макароны,чай},{котлета,макароны,кофе},{сосиска,макароны,кофе},{сосиска,макароны,чай}}
(4 строки)
```
Конечно, такая длинная выдача не помещается в стандартную ширину терминала.

Попробуем сделать разные выборки.
Посмотрим, что Иван предпочитает в свой второй рабочий день (в пятницу):
```SQL
SELECT pilot_name, schedule[2], meal[2:2][1:3] FROM pilots_new
WHERE pilot_name = 'Ivan';
```
Выдача:
```
 pilot_name | schedule |           meal           
------------+----------+--------------------------
 Ivan       |        5 | {{котлета,макароны,чай}}
(1 строка)
```
Все правильно.
Посмотрим, что едят пилоты, которые летают по средам. Для этого сделаем срез 
нужного дня, определив его индекс у каждого из пилотов:
```SQL
SELECT 
    pilot_name,
    schedule,
    meal[array_position(schedule, 3):array_position(schedule, 3)][1:3] as wen_meal
FROM pilots_new                               
WHERE array_position( schedule, 3 ) IS NOT NULL;
```
Результат:
```
 pilot_name | schedule  |         wen_meal          
------------+-----------+---------------------------
 Pavel      | {2,3,4,5} | {{сосиска,каша,кофе}}
 Boris      | {1,3,5,7} | {{котлета,макароны,кофе}}
(2 строки)
```
Все получилось корректно.

Посмотрим на рацион пилотов, которые работают в пятницу и хотя бы в один 
из выходных дней (сб или вс):
```SQL
SELECT
    pilot_name,
    meal[array_position(schedule, 5):array_position(schedule, 5)][1:3] as fri_meal,
    CASE 
        WHEN array_position(schedule, 6) IS NOT NULL 
        THEN meal[array_position(schedule, 6):array_position(schedule, 6)][1:3]
        ELSE '{"Не работает в субботу"}'::text[]
    END AS sat_meal,
    CASE 
        WHEN array_position(schedule, 7) IS NOT NULL 
        THEN meal[array_position(schedule, 7):array_position(schedule, 7)][1:3]
        ELSE '{"Не работает в воскр"}'::text[]
    END AS sun_meal
FROM pilots_new 
WHERE array_position(schedule, 5) IS NOT NULL
AND schedule && ARRAY[ 6, 7 ];
```
Результат:
```
 pilot_name |         fri_meal          |         sat_meal          |         sun_meal          
------------+---------------------------+---------------------------+---------------------------
 Ivan       | {{котлета,макароны,чай}}  | {{котлета,каша,чай}}      | {{сосиска,макароны,кофе}}
 Petr       | {{сосиска,макароны,чай}}  | {"Не работает в субботу"} | {{сосиска,каша,кофе}}
 Boris      | {{сосиска,макароны,кофе}} | {"Не работает в субботу"} | {{сосиска,макароны,чай}}
(3 строки)
```
Видим, что Иван работает и в пятницу, и в субботу, и в воскресение, а 
Петр и Борис - только в пятницу и в воскресенье.

Наконец, выведем пилотов, которые все 4 дня предпочитают макароны и ни разу не 
заказывают кашу. Для этого сделаем срез - все дни, второе блюдо:
```SQL
SELECT 
    pilot_name, 
    meal[1:4][2:2]::text[]
FROM pilots_new
WHERE NOT ( meal[1:4][2:2] @> '{ "каша" }' )
```
Выдача:
```
 pilot_name |                     meal                      
------------+-----------------------------------------------
 Boris      | {{макароны},{макароны},{макароны},{макароны}}
 Ivan       | {{макароны},{макароны},{макароны},{макароны}}
(2 строки)
```
Видим, что у нас два таких пилота.

Теперь обновим некоторые строки. Заменим третий рабочий день Петра с пятницы на среду,
также обновив его обед:
```SQL
UPDATE pilots_new
SET 
    schedule[3] = 3,
    meal[3:3][1:3] = '{{ "котлета", "макароны", "кофе" }}'::text[][]
WHERE pilot_name = 'Petr';

SELECT * FROM pilots_new
WHERE pilot_name = 'Petr';
```
Результат:
```
 pilot_name | schedule  |                                           meal            
                               
------------+-----------+-----------------------------------------------------------
-------------------------------
 Petr       | {1,2,3,7} | {{котлета,каша,кофе},{котлета,макароны,чай},{котлета,макар
оны,кофе},{сосиска,каша,кофе}}
(1 строка)
```
Теперь поменяем у Павла первый день со вторника на понедельник:
```SQL
UPDATE pilots_new
SET 
    schedule[1] = 1,
    meal[1:1][1:3] = '{{ "сосиска", "макароны", "чай" }}'::text[][]
WHERE pilot_name = 'Pavel';

SELECT * FROM pilots_new
WHERE pilot_name = 'Pavel';
```
Результат:
```
 pilot_name | schedule  |                                          meal             
                              
------------+-----------+-----------------------------------------------------------
------------------------------
 Pavel      | {1,3,4,5} | {{сосиска,макароны,чай},{сосиска,каша,кофе},{сосиска,макар
оны,кофе},{котлеты,каша,чай}}
(1 строка)
```
Все получилось, как мы хотели.

P.S. В ходе работы в документации было обнаружено, что функции 
`array_remove` и `array_prepend`, `array_append` работают только для 
одномерных массивов.

---
### **Задание 35.** <a name="задание_35"></a>

<details>
<summary>🔽 Развернуть задание 🔽</summary>
Задание:

Изучая приемы работы с типами JSON, можно, как и в случае с массивами, 
пользоваться способностью команды SELECT обходиться без создания таблиц.

Покажем лишь один пример. Добавить новый ключ и соответствующее ему 
значения в уже существующий объект можно оператором `||`:
```SQL
SELECT '{ "sports": "хоккей" }'::jsonb  '{ "trips": 5 }'::jsonb;
```
```
?column?
----------------------------------
{"trips": 5, "sports": "хоккей"}
(1 строка)
```

Для работы с типами JSON предусмотрено много различных функций и 
операторов, представленных в разделе документации 9.15 «Функции и операторы
JSON». Самостоятельно ознакомьтесь с ними, используя описанную технологию
работы с командой SELECT.
</details>

Ответ:

Попробуем создать более глубокую структуру типа JSON и обратиться к ней. Допустим, на
1 уровне у нас будет ключ `sports`, по которому будут лежать виды спорта. На 
2 уровне по названиям видов спорта будет храниться признак - занимается ли им человек:
```SQL
SELECT '{"sports": {"football": "y", "hockey": "n"}}'::jsonb
-> 'sports' -> 'footbal' as ftbl_flg;
```
Вывод:
```
 ftbl_flg 
----------
 "y"
(1 строка)
```
Успешно получилось "развернуть" структуру JSON.
С помощью другого оператора (`->>`) можно вывести то же самое, но в формате 
`text`:
```SQL
SELECT '{"sports": {"football": "y", "hockey": "n"}}'::jsonb
-> 'sports' ->> 'football' as ftbl_flg_text;
```
```
 ftbl_flg_text 
---------------
 y
(1 строка)
```
Как видно, двойные кавычки пропали.

Можно указать путь к нужному объекту другим способом с помощью операторов 
`#>` и `#>>`:
```SQL
SELECT '{"sports": {"football": "y", "hockey": "n"}}'::jsonb 
#> '{sports, football}';
```
```
 ?column? 
----------
 "y"
(1 строка)
```
`#>>` вернет тип `text`:
```SQL
SELECT '{"sports": {"football": "y", "hockey": "n"}}'::jsonb 
#>> '{sports, football}';
```
```
 ?column? 
----------
 y
(1 строка)
```

Можно проверить, присутствуют ли какие-либо строки массива в качестве ключей 
верхнего уровня с помощью оператора `?|`
```SQL
SELECT '{"sports": {"football": "y", "hockey": "n"}}'::jsonb ?| ARRAY['sports'];

 ?column? 
----------
 t
(1 строка)
```
Получится даже если одного из ключей нет в объекте JSON:
```SQL
SELECT '{"sports": {"football": "y", "hockey": "n"}}'::jsonb 
?| ARRAY['sports', 'hobbies'];

 ?column? 
----------
 t
(1 строка)
```
Однако есть оператор `?&` для проверки того, все ли ключи есть на верхнем уровне:
```SQL
SELECT '{"sports": {"football": "y", "hockey": "n"}}'::jsonb 
?& ARRAY['sports'];

 ?column? 
----------
 t
(1 строка)

SELECT '{"sports": {"football": "y", "hockey": "n"}}'::jsonb 
?& ARRAY['sports', 'hobbies'];

 ?column? 
----------
 f
(1 строка)
```
Но если нужные ключи лежат глубже верхнего уровня, любой из них 
вернет значение false:
```SQL
SELECT '{"sports": {"football": "y", "hockey": "n"}}'::jsonb 
?| ARRAY['football, hockey'];

 ?column? 
----------
 f
(1 строка)
```
С помощью оператора `-` (минус) можно удалить пару ключ/значение или элемент-строку 
из левого операнда. Пары ключ/значение выбираются по значению ключа. Немного 
усложним структуру нашего JSON-массива, добавив информацию про хобби:
```SQL
SELECT '{"sports": {"football": "y", "hockey": "n"}, 
         "hobbies": {"music": "y", "reading": "n"}}'::jsonb         
- 'sports';

                  ?column?                   
---------------------------------------------
 {"hobbies": {"music": "y", "reading": "n"}}
(1 строка)
```
Либо можно выбрать сразу несколько полей:
```SQL
SELECT '{"sports": {"football": "y", "hockey": "n"}, 
         "hobbies": {"music": "y", "reading": "n"}}'::jsonb         
- '{sports, hobbies}'::text[];

 ?column? 
----------
 {}
(1 строка)
```
Получился пустой объект JSON.

Также можно указывать путь к удаляемому объекту с помощью оператора `#-`:
```SQL
SELECT '{"sports": {"football": "y", "hockey": "n"}, 
         "hobbies": {"music": "y", "reading": "n"}}'::jsonb         
#- '{sports, football}';

                                ?column?                                
------------------------------------------------------------------------
 {"sports": {"hockey": "n"}, "hobbies": {"music": "y", "reading": "n"}}
(1 строка)
```
Видно, что для работы с массивами JSON есть большое число операторов и функций.

---